# Banco de Dados
# Para Vercel Postgres, use a POSTGRES_URL fornecida no dashboard da Vercel
# Formato: postgres://usuario:senha@host:5432/database?sslmode=require
DATABASE_URL=

# Ambiente de Execução
NODE_ENV=production

# Porta (Render define automaticamente como 10000, localmente use 5000)
PORT=5000

# Segurança da Sessão
# Gere uma chave forte usando: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=

# Configurações do Replit (somente para Replit)
# REPLIT_DOMAINS=
# ISSUER_URL=
# REPL_ID=
